## 新增文件

|路径|作用|
|---|---|
|[`urdf/taili_quad/__init__.py`](vscode-file://vscode-app/Applications/Cursor.app/Contents/Resources/app/out/vs/code/electron-sandbox/workbench/urdf/taili_quad/__init__.py)|包根路径、`URDF_PATH`、12 关节名、足端 link、对称质量配对|
|[`scripts/verify_urdf.py`](vscode-file://vscode-app/Applications/Cursor.app/Contents/Resources/app/out/vs/code/electron-sandbox/workbench/scripts/verify_urdf.py)|静态检查入口（仅标准库，不依赖 Genesis）|

## 运行方式

cd /Users/Admin/project/genesis-world/taili

python scripts/verify_urdf.py

可选参数：`--urdf` 指定 URDF；`--out-dir` 指定报告目录（默认 `urdf/taili_quad`）。

## 检查项（15 项）

与 [`pipeline.md`](vscode-file://vscode-app/Applications/Cursor.app/Contents/Resources/app/out/vs/code/electron-sandbox/workbench/pipeline.md) 阶段一一致，包括：

- XML / 拓扑 / joint 引用
- 质量、惯性、正定性
- 12 个 `revolute` 关节限位与轴向
- `base_link`、四足关节与足端 link
- 17 个 mesh 文件是否存在
- 左右对称腿质量偏差（≤5%）
- 新增 `mass_inertia_sanity`：质量与惯性对角元范围

## 当前结果

刚跑过一次：15/15 PASS，连杆总质量约 38.98 kg。报告已更新：

- `urdf/taili_quad/verify_report.txt`
- `urdf/taili_quad/verify_report.json`

有 FAIL 时退出码为 1；仅有 WARN 时仍为 0，但报告结论会标为 WARN。

其他脚本可通过 `from urdf.taili_quad import URDF_PATH, ACTUATED_JOINT_NAMES` 复用同一 URDF 路径与关节名。需要的话我可以再写一个 Genesis 可视化加载脚本（阶段一第 2 步）